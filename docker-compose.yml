services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3111:8000"
    volumes:
      - ./periodt_data:/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  periodt_data:
    driver: local
