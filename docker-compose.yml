services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3111:8000"
    volumes:
      - ./periodt_data:/data
    restart: unless-stopped
    environment:
      - TZ=UTC
      - NOTIFY_DAYS_BEFORE=3
      - NOTIFY_HOUR=9
